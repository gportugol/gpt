SUBDIRS = man

# Issue #10 -- a fonte do manual.
#
# O PDF publicado na release e gerado a partir deste .tex pelo CI, mas o
# arquivo nao estava em lugar nenhum da distribuicao: o tarball trazia o
# diretorio doc/ sem a unica coisa que permite reconstruir a documentacao.
#
# gpt.kilepr e arquivo de projeto do Kile (IDE) e fica de fora de
# proposito -- ver stuff/check-dist.sh.
EXTRA_DIST = manual/manual.tex
